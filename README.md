# Mova

`Mova` is a Flutter client for creation workflows powered by Seedance and
AgentEarth-compatible APIs.

## Run

```bash
flutter pub get
flutter run
```

## Build

```bash
flutter build apk --release
flutter build ios --no-codesign
```
